# 团队协作：直接更新 main

有写入权限的成员直接在 main 提交、推送，不新建分支、不创建 PR、不等待负责人或其他用户审批。陌生用户不会因此获得写入权限。

## 同事复制这句话给 AI

> 我要改进并推送这个 Skill：……。请检查官方最新版，保留本地修改，同步最新 main，直接修改、测试和推送 main，不新建分支、不创建 PR。不上传生成图片、人物控制图、换装图、真人参考照片、客户资料或密钥。版本号递增，在更新记录及提交说明中写清改动、原因、影响、实际测试结果和风险。出现冲突先说明，不覆盖别人的修改、不强推。

只反馈、不想自己修改的同事，可直接在仓库 Issues 中描述问题；不必获得写入权限。需要提交修改的同事使用各自 GitHub 账号，由负责人逐个邀请，绝不共享账号或令牌。

## 版本规则

1. `VERSION` 使用语义化版本 `X.Y.Z`。不兼容的流程或接口变化增加主版本；向后兼容的新能力增加次版本；向后兼容的修复增加修订版本。
2. 每次准备发布都必须把 `VERSION` 提升到高于 main 现有版本的新号码；版本号只增不减、不复用，已发布标签不可移动或覆盖。
3. 每个 `VERSION` 都必须在 `CHANGELOG.md` 中有同名标题，并逐项列明该版本的实际更新内容。不得只写“优化”“更新”等无法核实的概括，也不得虚构未完成或未测试的内容。
4. 更新记录应覆盖对使用者有意义的新增、变更、修复、兼容性影响和已知限制；没有实际内容或没有对应更新记录时不得发布。
5. 发布说明使用该版本的更新记录。修复已发布版本时创建更高的新版本，不删除历史 Release，不复用旧标签。

## 提交与发布

1. 检查工作区、实际分支和官方 main。干净且可快进时同步；若在旧分支，先检查独有提交与未提交修改，不能丢弃或擅自整支合入。无法安全同步时暂停说明，不新建分支绕过问题。
2. 本次功能改动必须同时递增 `VERSION` 并更新 `CHANGELOG.md`。运行仓库检查及相关测试，检查无关改动和隐私资料；视觉测试仍需明确授权。只暂存本次改动，根据暂存 diff 自动填写提交标题和正文：原因、改动、影响、实际测试结果、未测试部分和风险，不虚构通过结果。
3. 用户授权提交推送后直接推送 main，不创建 PR、不请求审批。仅修改或本地提交不授权推送。并发更新导致拒绝推送时重新获取远程，保留双方内容，重新核对版本号和测试；冲突暂停，不强推，不复用已有标签。
4. 合入 main 后运行自动检查；检查成功后创建与 `VERSION` 相同的 Git 标签和 GitHub Release，并以该版本在 `CHANGELOG.md` 中列明的更新内容作为发布说明。
5. 推送后报告提交链接、自动检查及发布的真实状态。检查失败不等于推送失败；发现问题用新的修复提交恢复稳定内容，版本号继续增加，不删除历史。普通人物控制图或换装制作不自动提交发布。

main 取消 PR 审批、CODEOWNERS 审批及推送前必过检查门槛，保留禁止强推和删除 main。自动检查仍在推送后运行，成功后才由既有流程发布 Release。自动提交说明是 AI 使用本 Skill 时执行的流程，不是任意 Git 客户端的后台钩子。

## 自动检查的边界

检查 Markdown 相对链接、版本号、更新记录、Skill 基本结构，并拦截常见输出目录、真人素材、私密文件和明显密钥模式。它只能帮助发现问题，不能保证识别所有隐私、秘密或画面质量；提交者仍须检查变更清单。任何允许入库的示意素材也必须确认拥有分享权。
