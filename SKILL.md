---
name: yzzx-ip-control-sheet
description: 为同一真人角色创建高写实人物控制图，并在用户确认身份还原后，依据服装参考图生成多套换装控制图。适用于人物三视图、面部与表情设定、角色一致性修复及真人换装参考图；不适用于虚构人物的自由设计或普通单张人像美化。
---

# 人物控制图与换装

严格按阶段推进。不要在必需资料未齐时生成，也不要把控制图确认与换装合并进行。

在首次生成、局部修复或换装前，阅读 [references/prompt-contracts.md](references/prompt-contracts.md)，并把其中相应的提示词契约完整落实到图像生成或编辑指令中。

## 生图前版本检查（所有宿主）

每次人物控制图任务首次生图前，按 [版本检查流程](references/version-check.md) 检查 [官方仓库](https://github.com/tychowu/yzzx-ip-control-sheet) 的最新正式 Release 及项目 main；覆盖新建、编辑、重生成、局部修复和换装，同一任务的多张生成及质量重试不重复询问。

全局 Skill 仅在检测到更新的正式 Release 时自动备份并同步到该 Release 的确切提交，展示版本、发布时间及该版本列明的更新内容；项目 main 有更新时仍展示提交时间及更新摘要并询问，用户明确要求 pull 除外。普通 pull 只更新项目副本，不同步全局。无新版 Release 时全局保持不动；发现本地修改、来源或版本关系不明时暂停。无法联网或识别版本时如实说明，并询问是否继续使用当前版本。完成检查前不得调用生图工具。

## 阶段一：建立人物资料

1. 先只询问人物名字。名字将作为图纸上唯一文字。
2. 获得名字后，询问人物身高（厘米）。用身高与参考照片共同判断自然头身比例；身高不是拉长身体或改变体型的许可。
3. 分三轮、一次只索取一类照片，并说明当前需要的照片：
   - 第一轮：清晰全身照。优先收集正面、正侧面、45°斜侧面和背面；自然站立、无遮挡、不过度广角、头脚完整。这些照片用于同时锁定肩宽、躯干厚度、手臂和腿部粗细、头身比例与姿态。
   - 第二轮：清晰脸部特写。优先正面、正侧面和45°角，光线均匀、无强滤镜、无遮挡。
   - 第三轮：清晰表情特写。至少覆盖用户希望精准复现的微笑、大笑、惊讶和厌恶等表情，用来学习眼睛、眉毛、嘴形、下颚与面部肌肉的联动。
4. 每轮收到后先检查可用性，再进入下一轮。若关键部位模糊、遮挡、过曝、滤镜过强或分辨率不足，只补收该轮必要照片。
5. 三轮照片与身高齐全前，不开始生成。

不要声称能保证数学意义上的“100%相同”。应以参考图为唯一身份依据，尽最大可能锁定骨骼结构、五官比例、眼距、下颚几何、年龄感、肤色与可识别特征；不要擅自理想化、瘦脸、大眼、改鼻、改颌或改变体型。

## 阶段二：生成主控制图

三轮资料齐全后，按 [references/production.md](references/production.md) 执行模块化制作：原始照片整理 → 独立制作脸部角度、每个表情和每个全身视图，并裁出五官细节 → 展示独立图片供用户确认 → 只修改用户指出的问题图片并再次确认 → 全部待用图片确认后，确定性排版合成。最终交付一张高分辨率、白色背景、摄影棚均匀布光的横向控制图，人物统一穿无标识黑色休闲短袖与黑色长裤。

所有待用图片须先获得用户确认，再进行整张控制图拼合。展示独立图片时，在图片外使用稳定编号和名称（角度 A1–A3、表情 E1–E8、五官 D1–D5、全身 B1–B3），让用户可直接指出哪几张有问题；不要把编号写入图片，不为审核提前制作整张预览拼版。用户可一次确认整批，也可分批确认。记录各图片的具体版本及待确认／需修改／已确认状态，代理自行验收不等于用户确认，未提及的图片不自动视为已确认。

用户指出问题后，只重做对应图片，展示修改版并等待确认；其余已确认图片继续保留。仅当本次拼合所需的全部图片都已确认时，才将新确认的修改版与其他已确认图片一起拼合。不得每改一张就自动重新拼版。纯裁切、尺度或排版调整可先展示相关独立图片的调整效果，确认后再拼合，无需重生成。

禁止“先生成整张控制图，再反复整图重绘表情或皮肤”的串行流程。生成模型的保持不变提示不等于像素锁定。每个模块始终使用原始真人照片作为身份依据；最终拼版不再交给生成模型润色。此流程降低累计漂移，但不保证生成的人脸完全相同。

控制图必须包含以下无文字标签的画面，并使用固定阅读顺序：

- 脸部角度：正面、侧脸、45°角。
- 五官细节：眼睛与眉毛合并为一张、单只耳朵一张、鼻子一张、闭合嘴唇一张、露齿嘴部一张。皮肤颜色与质感在这些真实微距中呈现，不单独增加皮肤贴图。
- 表情按从左到右、从上到下固定为：①平静自然；②自然露齿微笑；③张嘴大笑；④惊讶；⑤思考；⑥厌恶；⑦严肃；⑧半眨眼微笑。
- 全身：正面、侧面、背面；自然站姿、同一比例、完整头脚。

八张表情特写全部使用正脸方向：头部端正、面部朝向镜头，双眼连线水平，不歪头、不低头或仰头、不转头、不使用侧脸或45°角。使用一致的头肩构图和脸部尺度，手部不得入镜，不添加托腮、扶额、摸脸等动作。表情通过眉、眼、鼻、嘴及面部肌肉呈现；参考图有头部倾斜或手势时，只参考表情特征，将头部方向规范为正面并去掉手势。此规则仅限八张表情图，不影响上方三角度人像及全身三视图。

表情约束：第②张应是轻柔、自然的露齿微笑，不得与第③张混成同一种张嘴大笑；第③张大笑时双眼应自然睁开，不得一睁一闭或误做眨眼；第④张惊讶的嘴形、唇形、开口宽度和下颚下降必须优先服从惊讶参考图；第⑥张厌恶应优先复现厌恶参考图中的皱鼻、上唇抬起、眉眼收紧及嘴角变化，保持本人五官结构；无对应参考时仅作自然、克制的推断，不沿用旧的困扰表情照，也不能简化成生气或严肃。第⑧张才允许出现单眼半眨。

若参考人物佩戴眼镜，控制图中保留参考眼镜的镜框形状、尺寸、颜色和佩戴位置，并去除遮挡眼睛的镜片眩光、白色反光斑及明显环境倒影，使双眼和眼神清晰可见。正面角度及八张表情中的镜框必须水平居中，左右镜片、镜腿与鼻托位置对称稳定，不得高低不一、偏移或歪斜；侧面和45°角遵循真实透视与遮挡关系，但眼镜仍须端正贴合面部，不得变形、悬浮或穿插。各模块中的眼镜款式和位置保持一致。

版式固定为横向左右分区：

- 左上：三张大幅上半身角度图，依次为正面、侧面、45°角。拼合时统一图幅宽高和宽高比，并按头顶至下巴的高度统一人物大小比例、对齐头顶与裁切底边；适当裁去多余身体或留白，只等比缩放，不拉伸五官或身体。
- 左下：八张等大的表情图，严格排成 4×2 网格并按上述顺序排列。放大脸部，仅显示领口向上的内容（保留完整头发、脸部、颈部和少量领口），不保留大面积胸腹或手臂；统一脸部尺度、图幅尺寸和留白。
- 右上：五张不重复的五官微距图；眼眉、耳、鼻位于上排，闭合嘴唇和露齿嘴部位于下排。不得出现重复眼睛、重复耳朵、重复嘴唇或额外皮肤贴图。
- 右下：全身正面、侧面、背面三视图，同一基线、同一高度、同一人物几何比例。名字固定在右下角安全区。

各区留出干净间距，不得重叠。整张图除人物名字外不得出现任何文字、字母、数字、水印、品牌标识或伪文字。名字必须逐字准确；如果生成器无法可靠写对文字，先生成无字图，再用可靠的排版方式只添加该名字。

全身三视图不得使用泛化模特体型。共同依据正面、侧面、斜侧面和背面参考图，锁定同一套肩宽与肩斜、胸廓宽度和厚度、腹部深度、上臂粗细、躯干长度、腰臀宽度、大腿与小腿粗细、腿长、头部大小和自然姿态。身高只用于辅助尺度判断；头身比例必须由身高与照片共同确定。正面和侧面全身像的小尺寸脸仍须分别匹配正面及侧脸特写；背面须匹配后脑轮廓和肩宽。禁止窄肩、过度瘦身、拉长腿部、缩小躯干、放大头部或三视图体型不一致。

全图使用统一的中性白平衡与曝光，使脸、耳、颈、手臂和手的肤色连贯，但保留解剖与光线造成的自然细微变化。禁止斑驳色块、局部橙红或灰暗、色带、重复毛孔，以及迷宫状、指纹状、虫状、浮雕状或平铺复制的线性皮纹。真实皮肤应包含随机且符合尺度的毛孔、细纹、细小绒毛、胡茬、微血管变化、少量自然瑕疵和柔和皮下散射，不能通过磨皮把问题抹平。

生成后逐项检查：版式是否符合固定区域；五官是否恰好五张且无重复；表情顺序与眼口动作是否正确；全身三视图是否共享相同肩宽、体型和用户身高对应的自然头身比例；全身小脸是否与脸部参考一致；手脚是否完整；服装是否统一；名字是否正确；是否存在额外文字；皮肤、头发、牙齿和眼睛是否出现塑料感、涂抹、蜡像感、斑驳色块、线状假皮纹、重复纹理或锐化光晕；佩戴眼镜时是否无明显镜片反光、双眼清晰，且镜框端正、对称、未歪斜或变形。若有明显生成缺陷，先针对单一缺陷自行重试一次；若重试使其他区域退化，保留较好版本并向用户说明，避免无止境整图重生成。

## 阶段三：确认与局部修复

展示主控制图并询问用户是否确认。提醒用户可以按画面位置指出不还原的局部，例如“第一排右侧的 45°脸”或“表情区第二排左侧”。

用户要求局部调整时：

- 编辑对应的独立模块，先单独展示修改版供用户确认；本轮全部修改图片确认后，再通过排版程序替换对应格子，与其他已确认图片一起拼合。可用真正蒙版时仅修改模块内指定部位。
- 只修改用户点名的格子或身体部位；其他格子的像素、构图、服装、光线、名字和版式保持不变。
- 继续使用相关原始照片作为身份参考，不得以最近一次生成图取代真人参考。
- 每轮只处理用户明确提出的变化；先确认独立修改图片，后拼合并展示整图。完整控制图的确认仍用于进入换装阶段。
- 改排版只移动和缩放已有模块；改全身几何只重做受影响的全身模块；统一肤色使用可回退的色彩调整。上述操作均不默认触发整图生成。逐像素不变只适用于经过合成前后比对确认的未修改区域。
- 用户仅要求上述大小比例、图幅或特写范围调整时，直接从已有高分辨率模块重新裁切、等比缩放和确定性拼版，不进行二次重绘，不重新生成脸部、皮肤或表情。保留已确认版本，输出新版本；未点名的五官、全身和姓名区域保持原样并做像素比对。

若人物呈现女性化外观，可做非常轻微、自然的皮肤整理，但必须保留毛孔、细纹和真实质感，不改变五官与脸型。只有当参考图明显欠曝或用户明确提出时，才可轻微提升面部曝光；保持原生肤色倾向与全身肤色均匀，禁止以审美理由改变族裔相关肤色。

没有用户明确确认“控制图可以”之前，不进入换装阶段。

## 阶段四：生成换装控制图

主控制图确认后，请用户上传服装参考图；每批最多 5 张。超过 5 张时，请用户分批上传。检查可见服装的颜色、廓形、面料和关键细节。服装或鞋履未完整展示时，依据可见服装的风格、配色、季节和正式程度自动补足整套搭配，不为缺少裤子、鞋或背面照片暂停询问；补足部分属于合理推断，不声称是原图细节。所有换装造型必须穿适配鞋履，不赤脚；参考图赤脚或只穿袜子时也自动补鞋。可见衣物忠实保留，仅在关键可见款式本身无法辨认时补问。

若人物呈现女性化外观，在首次换装生成前必须询问发型处理方式：采用服装参考图中的发型，或按用户文字描述修改。未得到选择前不要擅自换发型。若人物不呈现女性化外观，默认沿用已确认控制图的发型，除非用户另有要求。

每套服装生成一张独立的换装控制图，沿用已确认主控制图的白底、棚拍光线、固定网格、人物比例和全身正面／侧面／背面视图。必要时增加能看清领口、袖口、面料、纹理或配件的细节格，但不得删除三视图。只改变服装，以及用户明确选择的发型；锁定人物的五官、骨骼、眼距、下颚、肤色、体型与年龄感。

服装颜色、款式、版型、材质、纹理、图案、缝线、层次与配件必须忠于参考。不要把参考模特的脸、身体或肤色迁移到人物身上。图上名字仍固定在右下角，且不得出现其他文字；服装本身确有文字或标识时，仅在参考图足够清晰且用户希望忠实保留的情况下还原。

每批完成后逐张展示并让用户确认。局部修复继续遵循阶段三的最小修改原则。

换装采用整图单次编辑：直接以用户已确认的完整人物控制图为底图，结合当前一套服装参考，一次性替换图中所有人像可见的服装，包括三角度上半身、表情格领口和全身三视图。不再逐张生成全身模块或裁切拼版。主控制图初建仍采用阶段二的模块化流程；此规则是换装阶段的明确例外。

多套服装分别从同一张已确认主控制图独立生成，每套一张；禁止以前一套换装结果作为下一套底图，避免累计重绘。优先一次完成每套，明显缺陷最多针对性重试一次；不为统一画质额外重绘。保留原确认版和各套独立输出。锁定版式、裁切尺度、名字、五官、表情、发型、体型和皮肤，只变服装及其配件，不迁移参考模特身份或手持道具。说明整图生成无法保证未改部位像素完全不变，生成后检查身份与皮肤；未通过则如实说明，不无止境重绘。

## 阶段五：整理交付文件

全部换装图完成后，把用户确认的原始人物控制图和所有正式换装图片整理到桌面新建文件夹。文件夹以角色人物名称命名，并明确说明内容，例如“{人物名字}-人物控制图与换装”。

- 只复制最终确认的原始人物控制图和正式换装图片；不放入旧版本、失败稿、中间模块或参考照片。
- 原始人物控制图命名为“{人物名字}-00-原始人物控制图-确认版.{扩展名}”。
- 换装图按生成顺序使用两位编号，并结合可辨识的服装描述命名，例如“{人物名字}-01-深蓝西装-白衬衫.{扩展名}”。描述优先包含主色、主要单品和关键内搭，保持简洁，不使用含糊的“图片1”或“最终版”等名称。
- 使用复制方式整理，保留项目内原文件；不得移动、覆盖或删除源文件。
- 新建文件夹或目标文件已存在时，不静默覆盖。先选择不冲突的版本化文件夹名，或在名称可能影响用户预期时请求确认。
- 完成后核对桌面文件夹中的文件数量、编号连续性、人物名称和服装描述，并向用户提供文件夹路径。

## 交互边界

- 每次只问当前阶段必需的问题，避免一次抛出长表单。
- 不把模糊推断当作人物身份、性别认同或真实肤色；相关判断仅用于视觉工作流，拿不准时用中性默认或简短询问。
- 不在用户未授权时公开、上传到无关服务或保存这些人物照片到工作流之外。
- 若工具无法同时接收全部参考图、无法局部编辑或无法可靠保持身份一致，明确说明限制，并采用最接近且可验证的工作方式，不假装已经做到。
- 若图像工具限制参考图数量，每次只输入当前模块需要的原始照片和该模块底图。必要时用非生成方式制作少量原图参考拼版，保持足够的脸部像素；拼版不会增加模型的实际细节容量，不能把所有照片缩成小图后宣称身份信息无损。

## Skill 改进与发布

团队修改与发布遵循 [CONTRIBUTING.md](CONTRIBUTING.md)。版本号见 [VERSION](VERSION)，每个版本的更新内容见 [CHANGELOG.md](CHANGELOG.md)。每次准备发布都必须递增语义化版本号，并在对应更新记录中逐项写清实际新增、变更、修复、兼容性影响和已知限制；没有对应更新内容的版本不得发布。仅在用户明确授权提交推送时发布，普通人物控制图或换装制作不自动修改、提交或发布仓库。
