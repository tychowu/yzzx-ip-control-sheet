# 更新记录

每个版本必须列明实际更新内容；没有更新记录的版本不得发布。

## 1.1.0 · 2026-09-16

- 将版本检查从“首次生图前”提前为调用 `yzzx-ip-control-sheet` 后必须执行的第一步，检查完成前不进入人物资料收集或制作流程。
- 根据实际加载路径识别本次调用的是项目级还是全局级 Skill，只检查并处理当前调用副本，不连带检查或更新另一份安装。
- 统一以 GitHub 最新正式 Release 为仓库版本；版本不同时列出当前版本、仓库版本、相差的正式版本数量，以及所有跨越版本的更新内容。
- 项目级和全局级发现版本差异后都先询问用户是否更新，不再自动同步全局 Skill；获得同意后仍须检查本地修改和安全更新条件。

## 1.0.0 · 2026-09-14

- 建立语义化版本管理：版本号统一记录在 `VERSION`，格式为 `X.Y.Z`。
- 增加 `CHANGELOG.md`，要求每次发布逐项说明实际更新内容。
- 增加仓库检查与 GitHub Release 自动发布流程；检查通过后按同版本创建不可覆盖的标签和 Release。
- 收录当前真人角色控制图基础流程：分轮收集参考、模块确认、确定性拼版、局部修复、整图换装及正式文件整理。
